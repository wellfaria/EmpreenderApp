<Window x:Class="EmpreendedoresApp.Views.ProdutosView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EmpreendedoresApp.Views"
        mc:Ignorable="d"
        Title="Meu Empreendimento | Produtos / Serviços" Height="450" Width="800">
   
        <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height ="Auto" />
            <RowDefinition Height ="Auto" />
            <RowDefinition Height ="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0"
               Grid.Column="0"
               Content="Nome do Produto"
               HorizontalAlignment="Right"
               VerticalAlignment="Center"
               FontSize="20"
               Margin="10"/>

        <TextBox Name="txtNomeProduto"
                 FontSize="20"
                 Grid.Row="0"
                 Grid.Column="1"
                 Margin="10"
                 HorizontalAlignment="Stretch"
                 VerticalAlignment="Center" />


        <Label Grid.Row="1"
               Grid.Column="0"
               Content="Preço do Produto"
               HorizontalAlignment="Right"
               VerticalAlignment="Center"
               FontSize="20"
               Margin="10"/>
            
        <TextBox Name="txtPrecoProduto"
                 FontSize="20"
                 Grid.Row="1"
                 Grid.Column="1"
                 Margin="10" 
                 HorizontalAlignment="Stretch"
                 VerticalAlignment="Center"/>

        <Button Name="btnAdicionarProduto"
                Grid.Row="2"
                Content="Adicionar Produto"
                FontSize="20"
                Background="LightGreen"
                Grid.ColumnSpan="2"
                HorizontalAlignment="Center"
                Margin="10"
                Padding="20,10" 
                Click="BtnAdicionarProduto_Click"
                />
                 <!-- Height="50"
                Width="200" -->
        
        <Button Name="btnExcluirProduto"
                Grid.Row="2"
                Content="Excluir Produto"
                FontSize="15"
                Background="PaleVioletRed"
                Foreground="WhiteSmoke"
                HorizontalAlignment="Left"
                Margin="10"
                Padding="20,10" 
                Click="BtnExcluirProduto_Click"/>


        <DataGrid Name="DataGridProdutos"
                  Grid.Row="3"
                  Grid.ColumnSpan="2"
                  Margin="10" 
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  AutoGenerateColumns="False"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  SelectionChanged="DataGridProdutos_SelectionChanged">
           
            



            <DataGrid.Columns>
                <DataGridTextColumn
				    Header="Id" Binding="{Binding Id}" Width="60" />
                <DataGridTextColumn
				    Header="Nome do Produto" Binding="{Binding Nome}" Width="*" />
                <DataGridTextColumn
				    Header="Preço Unitário" Binding="{Binding Preco, StringFormat={}{0:C}}"  Width="120" />
            </DataGrid.Columns>

        </DataGrid>

    </Grid>
</Window>
