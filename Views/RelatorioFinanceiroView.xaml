<Window x:Class="EmpreendedoresApp.Views.RelatorioFinanceiroView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EmpreendedoresApp.Views"
        mc:Ignorable="d"
        Title="Relatório Financeiro" Height="300" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Data inicial" FontWeight="Bold"/>
            
            <DatePicker x:Name="dpInicio"/>

            <TextBlock Text="Data final" Margin="0,10,0,0" FontWeight="Bold"/>
            <DatePicker x:Name="dpFim"/>
        </StackPanel>

        <Button Grid.Row="2"
                Content="Gerar Relatório em PDF"
                FontSize="18"
                FontStyle="Oblique"
                Height="40"
                Margin="0,20,0,0"
                VerticalAlignment="Top"
                Click="BtnGerarRelatorio_Click"/>
    </Grid>
</Window>
